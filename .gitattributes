# 统一换行，避免 Windows/Linux 来回改
* text=auto eol=lf

# Windows 专用脚本保持 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 跨平台脚本与 Gradle wrapper 用 LF（CI 在 Linux 上执行）
gradlew text eol=lf
*.sh    text eol=lf
*.ps1   text eol=lf

# 二进制
*.jar  binary
*.png  binary
*.jpg  binary
*.zip  binary
*.ogg  binary
